big todo im backend: aber das ist warhscheinlich nur kosmetik
die rückgabetypen ans frontend schön bennen =>

Added functionality:

    - add a print summery
    - add a last changed timestamt,

    -muss TemplateDetails noch anders machen dass es die sachen wie bei machinedetails alle im ui anzeigen 
    kann aber von außen gesteuert wird und alle funktionen als optional props uebergeben bekomme

    -TempalteListe kann schöbner aussehen

    => DB AUF POSTGRES UMSTELLEN!!